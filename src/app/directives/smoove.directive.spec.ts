import { SmooveDirective } from './smoove.directive';

describe('SmooveDirective', () => {
  it('should create an instance', () => {
    const directive = new SmooveDirective();
    expect(directive).toBeTruthy();
  });
});
